# Kivy Paddle Ball Game - Paddle Ball Game (Directory)

The location of all application files.

## Summary

This directory contains the source code directory and calls the game's constructor when invoked (via `__main__.py`).
